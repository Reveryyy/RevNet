<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.reveryy</groupId>
    <artifactId>revnet</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>revnet</name>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <build>
        <defaultGoal>clean package</defaultGoal>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.42</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.3</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- ========================================================= -->
            <!-- ====================== DEV ONLY ========================= -->
            <!-- ========================================================= -->
            <!--
                SVILUPPO LOCALE REVNET

                Quando viene eseguito:

                    mvn package

                Maven:
                    1. compila il plugin
                    2. crea lo shaded JAR
                    3. esegue lo script di sviluppo
                    4. lo script ferma Paper, se necessario
                    5. copia il nuovo JAR
                    6. riavvia Paper

                ⚠️ RIMUOVERE TUTTO IL BLOCCO "DEV ONLY"
                   PRIMA DI UNA BUILD DI PRODUZIONE.
            -->

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>

                <executions>

                    <!-- ================================================= -->
                    <!-- DEV ONLY: deploy + restart Paper                  -->
                    <!-- ================================================= -->

                    <execution>
                        <id>dev-deploy-and-restart</id>
                        <phase>package</phase>

                        <configuration>
                            <target>

                                <exec
                                        executable="cmd.exe"
                                        dir="${project.basedir}"
                                        failonerror="true">

                                    <arg value="/c"/>
                                    <arg value="&quot;${project.basedir}\dev\restart-server.bat&quot;"/>
                                    <arg value="${project.build.directory}\${project.build.finalName}.jar"/>

                                </exec>

                            </target>
                        </configuration>

                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>

                </executions>
            </plugin>

            <!-- ========================================================= -->
            <!-- =================== FINE DEV ONLY ======================= -->
            <!-- ========================================================= -->
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>

    <repositories>
        <repository>
            <id>spigotmc-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>1.21.4-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.42</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>dev.jorel</groupId>
            <artifactId>commandapi-spigot-core</artifactId>
            <version>12.0.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>dev.reveryy</groupId>
            <artifactId>revconfig</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>dev.reveryy</groupId>
            <artifactId>revdata</artifactId>
            <version>1.0.0</version>
        </dependency>
    </dependencies>

</project>
